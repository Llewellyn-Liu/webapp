<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lrl.liustationspring.dao.mapper.BootstrapMapper">
    <insert id="createEnvironment">
        drop table if exists User;
#         drop table if exists Morid;
        create table User(id int(6) unsigned AUTO_INCREMENT PRIMARY KEY, firstname varchar(20), lastname varchar(20),
        username varchar(50), password varchar(72), accountcreated datetime, accountlastmodified datetime,
        token varchar(72));
    </insert>


</mapper>